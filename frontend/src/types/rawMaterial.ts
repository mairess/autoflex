export type RawMaterialCreationType = {
  code: string;
  name: string;
  stockQuantity: number;
};

export type RawMaterialResponseType = {
  id: number;
  code: string;
  name: string;
  stockQuantity: number;
};
